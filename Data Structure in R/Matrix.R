# A Matrix in R, can be created using matrix() function
ex_matrix <- matrix(c(1:12), nrow = 3, ncol = 4)
print(ex_matrix)

cat("\n")
# Also, we can transpose it using t() function
print(t(ex_matrix))

a <- matrix(c(8, 0, 2, 5), ncol = 2)
print(a)